import { CityPatternDirective } from './city-pattern.directive';

describe('CityPatternDirective', () => {
  it('should create an instance', () => {
    const directive = new CityPatternDirective();
    expect(directive).toBeTruthy();
  });
});
