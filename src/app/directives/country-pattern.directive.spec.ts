import { CountryPatternDirective } from './country-pattern.directive';

describe('CountryPatternDirective', () => {
  it('should create an instance', () => {
    const directive = new CountryPatternDirective();
    expect(directive).toBeTruthy();
  });
});
