import { NamePatternDirective } from './name-pattern.directive';

describe('NamePatternDirective', () => {
  it('should create an instance', () => {
    const directive = new NamePatternDirective();
    expect(directive).toBeTruthy();
  });
});
