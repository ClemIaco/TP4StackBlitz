import { PostalCodePatternDirective } from './postal-code-pattern.directive';

describe('PostalCodePatternDirective', () => {
  it('should create an instance', () => {
    const directive = new PostalCodePatternDirective();
    expect(directive).toBeTruthy();
  });
});
